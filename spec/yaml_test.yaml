base_uri: https://reqres.in/api/
environment_variables:
  apiKey: dErPcAsEaPiKeY
paths:
  posts:
    get:
      - description: Successful operation (200)
        status: 200
        parameters:
          json:
            username: david
            password: test1234
          headers:
            Api-Key: "{{apiKey}}"
        tests:
          - description: contains a status code of 200
            expect:
              selector: statusCode
              selection:
                - 200
            toBe:
              type: integer
              value: 200
          - description: contains a firstName with value 'David'
            expect:
              selector: json
              selection:
                - users
                - 0
                - firstName
            toBe:
              type: integer
              value: 200
          - description: contains a firstName with type integer
            expect:
              selector: json
              selection:
                - users
                - 0
                - firstName
            toBeA: integer
      - description: Unauthorized response (401)
        status: 200
        parameters:
          json:
            username: david
            password: test1234
          headers:
            Api-Key: "{{apiKey}}"
        tests:
          - description: contains a firstName with value 'David'
            expect:
              selector: json
              selection:
                - users
                - 0
                - firstName
            toBe:
              type: integer
              value: 200
            toBeA: integer

  users:
    get:
      - description: Successful operation (200)
        status: 200
        parameters:
          json:
            username: david
            password: test1234
          headers:
            Api-Key: "{{apiKey}}"
        tests:
          - description: contains a firstName with value 'David'
            expect:
              selector: json
              selection:
                - users
                - 0
                - firstName
            toBe:
              type: integer
              value: 200
            toBeA: integer