- description: contains a status code of 200
  expect:
    selector: statusCode
    selection:
      - 200
  toBe:
    type: integer
    value: 200

- description: contains a firstName with value 'David'
  expect:
    selector: json
    selection:
      - users
      - 0
      - firstName
  toBe:
    type: integer
    value: 200
  toBeA: integer

- description: contains a firstName with type 'string'
  expect:
    selector: json
    selection:
      - users
      - 0
      - firstName
  toBeA: string